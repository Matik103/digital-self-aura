/**
 * Legacy export — RAG lives on the same project now.
 * Prefer `@/lib/functions` for all browser → backend calls.
 */
export const externalSupabase = null;
export const EXTERNAL_SUPABASE_URL = "";
export const EXTERNAL_SUPABASE_ANON_KEY = "";
