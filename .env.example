// Optional: only needed if you use the browser Supabase client directly.
// App UI calls Edge Functions through `/api/fn/*` and does not require these.
VITE_SUPABASE_PROJECT_ID=""
VITE_SUPABASE_URL=""
VITE_SUPABASE_PUBLISHABLE_KEY=""
